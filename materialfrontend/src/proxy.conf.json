/** Einstellungen für Proxyserver
 Durch die Verwendung von Docker wird hier backend-service statt einer IP-Adresse verwendet
 durch den Aufruf im Dockerfile wird die proxy.conf.json Datei eine Ebene höher verwendet
**/

{

  "/api/materialien": {
    "target": "http://backend-service:8080",
    "secure": false,
    "logLevel": "debug"
  },
  "/api/search": {
    "target": "http://backend-service:8080",
    "secure": false,
    "logLevel": "debug"
  }
}
